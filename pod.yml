apiVersion: v1
kind: Pod
metadata:
  name: nginx
  namespace: default
  labels:
    app1: frontend
    env: prod
spec:
  containers:
    - name: nginx
      image: nginx:1.14.2
      ports:
        - containerPort: 80

